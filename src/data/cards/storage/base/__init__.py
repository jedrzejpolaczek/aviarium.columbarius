"""Base abstractions and shared DuckDB utilities for the cards storage layer.

Provides the connection base (BaseStorage), transformation-tier base (TransformStorage),
and write primitives (DuckDBWriter) used by Bronze, Silver, and Gold storage
implementations.
"""
